import CodeBlock from '@theme/CodeBlock'

import Source from '!!raw-loader!./find-uppercase-files.sh'

# Find files with uppercase letters in their names

This can be quite handy for finding files that may cause issues on
case-insensitive file systems. 

<CodeBlock language="sh">{Source}</CodeBlock>
