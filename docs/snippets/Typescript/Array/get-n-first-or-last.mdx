import CodeBlock from '@theme/CodeBlock'

import Source from '!!raw-loader!./get-n-first-or-last.ts'

# Return the first or last `n` elements of an array

:warning: The default value of `n` is `1`.

<CodeBlock language="ts">{Source}</CodeBlock>
