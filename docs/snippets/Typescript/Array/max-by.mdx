import CodeBlock from '@theme/CodeBlock'
import Source from '!!raw-loader!./max-by.ts'

# Find the element in an array with the highest value for the specified key

<CodeBlock language="ts">{Source}</CodeBlock>
