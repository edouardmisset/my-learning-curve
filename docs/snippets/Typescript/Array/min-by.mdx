import CodeBlock from '@theme/CodeBlock'
import Source from '!!raw-loader!./min-by.ts'

# Find the element in an array with the lowest value for the specified key

<CodeBlock language="ts">{Source}</CodeBlock>
