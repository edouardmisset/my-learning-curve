import CodeBlock from '@theme/CodeBlock'
import Source from '!!raw-loader!./pipe.ts'

# Pipe & Compose

<CodeBlock language="ts">{Source}</CodeBlock>

A unary function is a function that accepts exactly one argument and can be
created by calling the function with just the first argument provided.
