import CodeBlock from '@theme/CodeBlock'
import Source from '!!raw-loader!./is-valid.ts'

# Check if a number is valid

In this context a valid number should be a finite number.  
`NaN`, `Infinity`, `-Infinity` are not valid.

<CodeBlock language="ts">{Source}</CodeBlock>
