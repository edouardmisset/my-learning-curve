---
import {
  type PackageManagersProps,
  PackageManagers as StarlightPackageManagers,
} from 'starlight-package-managers'

type Props = PackageManagersProps
---

<StarlightPackageManagers
  pkgManagers={['npm', 'yarn', 'pnpm', 'bun']}
  frame="terminal"
  icons
  {...Astro.props}
/>
