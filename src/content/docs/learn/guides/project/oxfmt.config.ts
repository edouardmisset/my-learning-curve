import { defineConfig } from 'oxfmt'

export default defineConfig({
  printWidth: 80,
  useTabs: false,
  tabWidth: 2,
  semi: false,
  singleQuote: true,
  arrowParens: 'avoid',
  trailingComma: 'all',
})
